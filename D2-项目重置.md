# ODPlatform D2: reset_project.py 的诞生

> **课程定位**: 实训第 2 天讲义
>
> **核心叙事**: 检验你是否真的学懂了 `init_project`,最好的办法,是让你写它的**反向操作**。今天我们做一个 `reset_project.py`——把 init 创建的一切清干净,让项目回到"git 刚 clone"的状态。听上去就是"init 建了啥我删啥",但你会看到,这个"反向"会连续撞 5 次墙;而每一面墙,都对应真实工程里"危险操作"必须配的一个设计模式。这些模式,你以后写**任何**带 `delete` / `remove` / `clean` / `reset` 名字的功能都用得上。
>
> **前置**: 完成 D1,Python ≥ 3.12,`pip install -e ./apps/platform` 已经能跑、`odp-init` 可用。
>
> **建议时长**: 3-4 课时

---

## 这门课怎么读(必读)

这一节的每一面墙,我都要你**亲手撞**——不是看我描述,是你自己打开终端,真的把 `docs/` 删掉、真的看 `git status` 满屏红、真的把 `.git` 删没。手敲一遍的痛感,是这门课的核心价值。

约定两个贯穿全文的标记:

- ⚡ **真撞**:看到它,**停下,打开终端,真的输命令**。
- 📺 **你会看到**:对一对你的实际输出和文档里写的是否一致。

节奏和 D1 一样:**先动手 → 看它在哪一步出事 → 现场勘验 → 修**。我不会在撞墙之前先把答案和解法名字告诉你;等你 5 次都撞完,文末有一张完整的推导链复盘,到时候回看会更通透。

跨平台命令沿用 D1 的约定:需要区分时用 🐧(Linux/macOS/Git Bash)/ 🪟(Windows PowerShell)并列给出;`git` / `python` / `pip` 这种两边一致的只写一次。

> 💡 **撞墙⑤有个跨平台彩蛋**: 同一个 bug,Windows 会大方报错(`WinError 32`),Linux 会**静默丢日志**。如果你日常用 Linux,强烈建议找台 Windows(或虚拟机)把撞墙⑤再撞一次——那一刻你会对"Windows 严格 / Linux 宽容"形成肌肉记忆。

---

## 引入:为什么要写 reset?

D1 学完,你很快会撞上一件烦人的事:**测试 `init_project` 是个反复劳动**。

你想再跑一遍 `odp-init` 看看效果,就得先把上次建的东西删掉:

```bash
rm -rf data/raw data/train data/val data/test
rm -rf models/checkpoints models/pretrained
rm -rf runs apps/platform/logging
# ……或者干脆 rm -rf 一大片,然后忘了自己到底删了哪些
```

这里有三个真实痛点:

- **删错**——手一抖,删到了代码或者珍贵数据
- **删漏**——下次 `init_project` 报"目录已存在",白跑一趟
- **半年后**——回头根本不记得当时该删哪些

教学场景更典型:你想**再撞一次墙**复习(比如再体会一次 D1 的"print 太弱"),却没有一个干净状态可以回滚。

> 🤔 **那就写个 `reset_project.py` 吧**——把 init 建的东西全清掉,让项目回到 git 刚 clone 时的样子。如果是你,**你会怎么开始?**

大多数人的第一反应是:

> "简单啊,`init_project` 建了什么我就删什么呗。"

这个直觉看起来对。但魔鬼在细节里——我们今天就来看,这个"反向操作"会怎么连撞 5 次墙,而且**每一次都让你亲手撞**。

---

## 准备工作

### 0.1 确认环境

```
python --version                 # ≥ 3.12
pip install -e ./apps/platform   # D1 已经装过,确认还在
odp-init                         # 跑一次,确认 D1 的 init_project 正常工作
```

### 0.2 ⭐ 先准备好"后悔药"

> ⚠️ **这是本节最重要的一步,别跳过。**
>
> 撞墙①和撞墙②会**真的删掉你项目里的文件和目录,包括 `.git`**。我们靠 git / 备份把它们救回来——如果你**什么后悔药都没准备**,出事就是真丢了。

开始之前,下面两个**至少做一个**:

**A(推荐)——推到远程仓库**

```bash
# 在 GitHub / Gitee 建一个空仓库,然后:
git remote add origin <你的远程 URL>
git push -u origin main
```

**B(备选)——本地完整备份**

🐧 Linux/macOS/Git Bash:
```bash
cd ..
cp -r ODPlatform ODPlatform.backup
cd ODPlatform
```

🪟 Windows PowerShell:
```powershell
Set-Location ..
Copy-Item -Recurse ODPlatform ODPlatform.backup
Set-Location ODPlatform
```

确认 A 或 B 至少做了一项,再往下走。

### 0.3 造一个"灾难现场"

撞墙④要让你亲身体会"删大目录时屏幕黑屏"的痛,撞墙⑤需要"已经存在的日志文件"。我们写一个一次性脚本,造一批假数据:几百个假标注、一个 2GB 的稀疏文件、5000 个小文件、一些假日志。

> 📌 这是为本课造的"假大数据"——2GB 是稀疏文件,**实际磁盘占用接近 0**。课程结束后正常 reset 一次就清掉了。

**用编辑器**创建 `scripts/make_disaster_data.sh`(🐧)和 `scripts/make_disaster_data.ps1`(🪟):

🐧 `scripts/make_disaster_data.sh`:
```bash
#!/bin/bash
# 为 D2 造灾难现场数据
set -e
echo "🎬 准备灾难现场..."

# 1. data/raw/ 假装放了珍贵标注(后面要保护它)
mkdir -p data/raw/precious_dataset/images data/raw/precious_dataset/labels
for i in $(seq 1 200); do
    echo "fake image $i" > "data/raw/precious_dataset/images/img_${i}.jpg"
    echo "0 0.5 0.5 0.3 0.4" > "data/raw/precious_dataset/labels/img_${i}.txt"
done
echo "  ✅ data/raw/precious_dataset/ — 400 个文件(模拟珍贵标注)"

# 2. runs/ 里造一个 2GB 稀疏文件(删它时会跑文件系统)
mkdir -p runs/exp_2026_05_10
dd if=/dev/zero of=runs/exp_2026_05_10/best.pt bs=1 count=0 seek=2G 2>/dev/null
echo "  ✅ runs/exp_2026_05_10/best.pt — 2 GB(稀疏文件)"

# 3. runs/ 里造 5000 个小文件(大量 inode,删起来真的慢)
mkdir -p runs/exp_2026_05_10/tb_logs
for i in $(seq 1 5000); do
    echo "step $i loss 0.${i}" > "runs/exp_2026_05_10/tb_logs/event.${i}"
done
echo "  ✅ runs/exp_2026_05_10/tb_logs/ — 5000 个小文件"

# 4. 一些已存在的日志(撞墙⑤的舞台)
mkdir -p apps/platform/logging/training/2026-05-10
for i in $(seq 1 50); do
    echo "training run $i log" > "apps/platform/logging/training/2026-05-10/run-${i}.log"
done
echo "  ✅ apps/platform/logging/ — 50 份训练日志"

echo "🎬 灾难现场准备就绪(总文件约 5650,名义约 2 GB)。"
du -sh data/raw runs apps/platform/logging 2>/dev/null
```

🪟 `scripts/make_disaster_data.ps1`:
```powershell
# 为 D2 造灾难现场数据
Write-Host "🎬 准备灾难现场..."

New-Item -ItemType Directory -Force -Path "data/raw/precious_dataset/images" | Out-Null
New-Item -ItemType Directory -Force -Path "data/raw/precious_dataset/labels" | Out-Null
1..200 | ForEach-Object {
    "fake image $_" | Set-Content "data/raw/precious_dataset/images/img_$_.jpg"
    "0 0.5 0.5 0.3 0.4" | Set-Content "data/raw/precious_dataset/labels/img_$_.txt"
}
Write-Host "  ✅ data/raw/precious_dataset/ — 400 个文件"

New-Item -ItemType Directory -Force -Path "runs/exp_2026_05_10" | Out-Null
fsutil file createnew "runs/exp_2026_05_10/best.pt" 2147483648 | Out-Null
fsutil sparse setflag "runs/exp_2026_05_10/best.pt" | Out-Null
Write-Host "  ✅ runs/exp_2026_05_10/best.pt — 2 GB"

New-Item -ItemType Directory -Force -Path "runs/exp_2026_05_10/tb_logs" | Out-Null
1..5000 | ForEach-Object { "step $_ loss" | Set-Content "runs/exp_2026_05_10/tb_logs/event.$_" }
Write-Host "  ✅ runs/exp_2026_05_10/tb_logs/ — 5000 个文件"

New-Item -ItemType Directory -Force -Path "apps/platform/logging/training/2026-05-10" | Out-Null
1..50 | ForEach-Object { "training run $_" | Set-Content "apps/platform/logging/training/2026-05-10/run-$_.log" }
Write-Host "  ✅ apps/platform/logging/ — 50 份日志"

Write-Host "🎬 灾难现场准备就绪。"
```

跑一遍:

🐧 `chmod +x scripts/make_disaster_data.sh && ./scripts/make_disaster_data.sh`
🪟 `.\scripts\make_disaster_data.ps1`

📺 **你会看到**(🐧):

```
🎬 准备灾难现场...
  ✅ data/raw/precious_dataset/ — 400 个文件(模拟珍贵标注)
  ✅ runs/exp_2026_05_10/best.pt — 2 GB(稀疏文件)
  ✅ runs/exp_2026_05_10/tb_logs/ — 5000 个小文件
  ✅ apps/platform/logging/ — 50 份训练日志
🎬 灾难现场准备就绪(总文件约 5650,名义约 2 GB)。
6.2M    data/raw
2.1G    runs
72K     apps/platform/logging
```

> 💡 **稀疏文件**: `dd ... seek=2G` 造的是稀疏文件,逻辑大小 2GB、实际磁盘占用接近 0;但 `shutil.rmtree` 删它时,文件系统照样要走完整删除流程,所以删起来是真的慢。这是教学造"假大文件"的标准技巧。

**把这个脚本提交了**——它是个会反复用到的工具(撞墙①④⑤都要它),而且等下 reset 会把 `scripts/` 删一遍,**提交过 git,才能在删掉后把它拉回来**:

```bash
git add scripts/make_disaster_data.sh scripts/make_disaster_data.ps1
git commit -m "chore: add disaster-data generator for reset drills"
```

> 💡 **如果你 0.2 选了 B(本地备份)**: 现在 `scripts/` 状态变了,重做一次备份,让它包含这次提交。(🐧 `cd .. && rm -rf ODPlatform.backup && cp -r ODPlatform ODPlatform.backup && cd ODPlatform`)

> 📎 **一个后面会反复用到的命令**: 接下来每次撞墙前,都要"先清掉运行时残骸 + 重新造灾难数据"。我把这两步记作 **〔造数据〕**,后面直接引用:
>
> 🐧 Linux/macOS 〔造数据〕:
> ```bash
> rm -rf data/raw/precious_dataset data/train data/val data/test \
>        runs models/checkpoints apps/platform/logging
> ./scripts/make_disaster_data.sh
> ```
> 🪟 Windows PowerShell 〔造数据〕:
> ```powershell
> Remove-Item -Recurse -Force -ErrorAction SilentlyContinue `
>     data/raw/precious_dataset, data/train, data/val, data/test, `
>     runs, models/checkpoints, apps/platform/logging
> .\scripts\make_disaster_data.ps1
> ```
>
> 用精准 `rm` 清残骸,**不要用 `git checkout .`**——那会把 paths.py 里还没提交的改动一起撤掉。

---

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 阶段 1: 朴素版 reset_project.py

## ▶ Step 1.1: 先动手——别想太多

📝 **任务**: 写一个 `reset_project.py`,把 `init_project` 创建的目录全删了。

**别看下面——你自己想 5 分钟,会怎么写?**

---

最直觉的写法:既然 `paths.py` 已经定义了所有要 init 的目录(`get_dirs_to_initialize()`),我直接**反过来 rm 一遍**不就行了?

**用编辑器**创建 `apps/platform/src/od_platform/cli/reset_project.py`:

```python
# apps/platform/src/od_platform/cli/reset_project.py  (v0)
import shutil
from od_platform.common.paths import ROOT_DIR, get_dirs_to_initialize


def reset_project() -> None:
    print("开始重置项目...")
    for d in get_dirs_to_initialize():
        if d.exists():
            print(f"删除: {d.relative_to(ROOT_DIR)}")
            shutil.rmtree(d)
    print("重置完成!")


if __name__ == "__main__":
    reset_project()
```

为了能跑,再加一个入口 `scripts/reset_project.py`:

```python
# scripts/reset_project.py
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "apps" / "platform" / "src"))

from od_platform.cli.reset_project import reset_project

if __name__ == "__main__":
    reset_project()
```

> 📌 **这个入口脚本先不提交**——它是我们刚写的、还在反复改的草稿。**先记住这件事**,撞墙①的时候它会变成一个让你印象深刻的细节。

## ▶ Step 1.2: ⚡ 真撞墙①——亲手把 docs/ 和 scripts/ 删掉

> ⚠️ 接下来你要**真删**。但别紧张——有 git 兜底(还有 0.2 节的远程/备份)。先做检查:

```bash
# 1. 已追踪文件没有任何改动
git diff --quiet && git diff --cached --quiet && echo "✅ tracked files clean"

# 2. 唯一允许的 untracked,就是 1.1 节刚建的 v0 + 入口
git status --porcelain
# 期望严格只有这两行(顺序无所谓):
# ?? apps/platform/src/od_platform/cli/reset_project.py
# ?? scripts/reset_project.py
```

⚡ **真撞**:确认干净后,跑朴素版:

```bash
python scripts/reset_project.py
```

📺 **你会看到**:

```
开始重置项目...
删除: data
删除: data/raw
删除: data/train
... (中间略)
删除: apps/platform/configs
删除: apps/platform/logging
删除: apps/platform/tests
删除: scripts
删除: docs
删除: docs/architecture
删除: docs/srs
删除: docs/code_review
重置完成!
```

**看最后几行**——`scripts/`、`docs/`、`apps/platform/tests/`、`apps/platform/configs/` 全被删了。注意 `删除: scripts` 这一行:**正在跑的脚本,把自己所在的目录删掉了。**

### 现场勘验:打开 git status

```bash
git status
```

📺 **你会看到**(彩色终端里满屏的红):

```
On branch main
Changes not staged for commit:
        deleted:    apps/platform/configs/.gitkeep
        deleted:    apps/platform/tests/__init__.py
        deleted:    apps/platform/tests/conftest.py
        deleted:    docs/architecture/ADR-001-monorepo.md   ← D1 写的架构决策
        deleted:    docs/architecture/README.md
        deleted:    docs/srs/README.md
        deleted:    scripts/init_project.py                 ← D1 写的入口
        deleted:    scripts/reset_project.py                ← 注意这一行!
        ...
```

几件值得停下来看的事:

1. `docs/architecture/ADR-001-monorepo.md` 没了——这是你 D1 辛苦写的架构决策记录。
2. `scripts/init_project.py` 没了——D1 写的入口脚本。
3. `apps/platform/tests/` 里 D1 写的测试代码全没了。

> 💭 **想象一下**: 如果这个项目刚好没用 git(初学者很常见),或者你昨天刚写了一份 `ADR-002` 还没 commit——**这一刀就把活儿白干了**。

### 灾难恢复:git 救场

幸好有 git。此刻 `paths.py` 没有任何未提交改动(我们还没动它),所以可以放心 checkout:

```bash
git checkout .          # 恢复所有被删的【已追踪】文件
git status
ls docs/                # ✅ ADR 回来了
ls apps/platform/tests/ # ✅ 测试代码回来了
ls scripts/             # ⚠️ 看仔细——下面有讲究
```

`docs/`、`tests/` 都回来了。但 `scripts/` 里**不是所有文件都救回来了**:

| 文件 | 状态 | 原因 |
|---|---|---|
| `scripts/init_project.py` | ✅ 在 | D1 已提交,checkout 救回 |
| `scripts/make_disaster_data.sh` / `.ps1` | ✅ 在 | 0.3 节我们提交过,checkout 救回 |
| `scripts/reset_project.py` | ❌ **没了** | 1.1 节建好后**没提交**,checkout 救不回 |

**这就是关键一课**——那 10 行入口脚本是**未追踪**的,真删 + `git checkout` 救不回 = 永久丢失。

> 💭 **未提交的工作 = 没有工作。** 这种"我刚改了点东西没 commit 就被毁了"的场景,工程师每天都在遇到。这是朴素版给你上的第一堂安全课。

把那 10 行入口**再粘一遍**(代价很小,就当复习):

```python
# scripts/reset_project.py
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "apps" / "platform" / "src"))

from od_platform.cli.reset_project import reset_project

if __name__ == "__main__":
    reset_project()
```

> 💡 `cli/reset_project.py`(v0)**没有**被删——因为朴素版删的是 `get_dirs_to_initialize()` 里那些目录(`SCRIPTS_DIR`、`DOCS_DIR`……),其中不含 `apps/platform/src/.../cli/`(那是源码目录,从来不在 init 列表里)。所以 v0 还原封不动地在原位。

灾难数据(`runs/`、`data/raw/` 那些)是 gitignored 的,checkout 救不回——**重新跑一次灾难脚本**(脚本本身因为提交过,一直在):

🐧 `./scripts/make_disaster_data.sh` &nbsp;&nbsp; 🪟 `.\scripts\make_disaster_data.ps1`

### 灾难总结:为什么会这样?

回头看 `get_dirs_to_initialize()` 返回的列表:

```python
def get_dirs_to_initialize() -> list[Path]:
    return [
        DATA_DIR, RUNS_DIR, MODELS_DIR, ...   # 运行时产物,不进 git
        CONFIGS_DIR,      # 进 git(.gitkeep + 模板)
        LOGGING_DIR,      # 不进 git
        UNIT_TEST_DIR,    # 进 git(测试代码)!
        SCRIPTS_DIR,      # 进 git!
        DOCS_DIR,         # 进 git!
    ]
```

> 💡 **关键洞察**: `get_dirs_to_initialize()` 是"**启动时要确保存在**的目录",里面**混着两种东西**——【运行时产物】和【git 追踪的目录】。
>
> `init_project` 用 `mkdir(exist_ok=True)`:目录已存在不报错,所以**无害**。
> 但 reset **反过来全删**,就把 git 追踪的目录也一起干掉了——**这是一个不对称的操作。**

`paths.py` 当初设计这个列表时,**没有区分"哪些是运行时产物、哪些是 git 目录"**——因为 init 不需要这个区分,但 reset 需要。

> 💡 **金句**: **init 和 reset 是不对称的——能 init 的,不一定能 reset。这是"危险操作设计"的第一课。**

## 🔧 怎么修?

不能盲目反向 init。**reset 得有自己的一份清单**——只删【真正的运行时产物】,绝不碰【git 追踪的代码/文档/测试】。

这份"reset 专用清单"长什么样、放哪儿,下一阶段动手。

> 📌 **v0 不提交**: 它是反面教材——留在 git 历史里,别人 checkout 出来跑一下就会误删数据。我们直接改造它。

---

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 阶段 2: 给 reset 一份自己的清单

## ▶ Step 2.1: 在 paths.py 里加 get_dirs_to_reset

打开 `apps/platform/src/od_platform/common/paths.py`,在 `get_dirs_to_initialize()` 下面加一个新函数:

```python
def get_dirs_to_reset() -> list[Path]:
    """
    返回 reset_project 可以安全清理的目录列表。

    和 get_dirs_to_initialize 的关键差异:
        - init:  "启动时要确保存在的目录"——可能含 git 追踪目录(如 docs/)
        - reset: "可以被清理的运行时产物目录"——【绝对不含】git 追踪目录

    安全原则:
        ✅ 只列【.gitignore 忽略的】运行时目录
        ❌ 任何 git 追踪的目录(代码/文档/测试/配置)绝不列入
        ❌ 用户的【原始数据】(RAW_DATA_DIR)绝不列入
    """
    return [
        # 划分后的数据集(由数据转换脚本产生,不进 git)
        TRAIN_DIR,
        VAL_DIR,
        TEST_DIR,
        # 训练产物(不进 git)
        RUNS_DIR,
        CHECKPOINTS_DIR,
        # 端私有运行时日志(不进 git)
        LOGGING_DIR,
        # 注意:以下目录【不在】 reset 范围内——
        #   RAW_DATA_DIR           : 用户辛苦准备的原始数据,reset 不动
        #   PRETRAINED_MODELS_DIR  : 预训练权重(很大,重下载耗时)
        #   CONFIGS_DIR / UNIT_TEST_DIR / SCRIPTS_DIR / DOCS_DIR : 都进 git
    ]
```

> 💡 **设计哲学——明示比暗示安全。** `get_dirs_to_reset()` 不是从 `get_dirs_to_initialize()` "减去"什么,而是**重新明确写出"哪些可以删"**。好处:看代码就知道边界,不用心算"减完剩啥";以后有人往 init 列表加新目录,reset 列表**不会**跟着自动变。

## ▶ Step 2.2: 改写 reset_project.py(v1)

```python
# apps/platform/src/od_platform/cli/reset_project.py  (v1)
import shutil
from od_platform.common.paths import ROOT_DIR, get_dirs_to_reset


def reset_project() -> None:
    print("开始重置项目...")
    for d in get_dirs_to_reset():
        if d.exists():
            print(f"删除: {d.relative_to(ROOT_DIR)}")
            shutil.rmtree(d)
    print("重置完成!")


if __name__ == "__main__":
    reset_project()
```

## ▶ Step 2.3: ⚡ 真撞——确认 v1 不再删 git 目录

执行 〔造数据〕,然后跑 v1:

```bash
# 〔造数据〕(见 0.3 节)
python scripts/reset_project.py
```

📺 **你会看到**——只有 6 个目录,不再碰 docs/ scripts/ tests/ configs/:

```
开始重置项目...
删除: data/train
删除: data/val
删除: data/test
删除: runs
删除: models/checkpoints
删除: apps/platform/logging
重置完成!
```

```bash
git status              # 关键:不应该有任何 deleted: 行
ls data/raw/            # ✅ precious_dataset/ 还在(不在 reset 列表)
ls docs/                # ✅ ADR 安然无恙
```

🎉 **代码、文档、原始数据都安全了。**

但是……

## ▶ Step 2.4: ⚡ 真撞墙②——故意把 ROOT_DIR 加进清单

> 💭 **思考题**: reset 现在不删 git 目录了,看起来安全。但你能想到一个**新的危险**吗?

危险藏在"清单本身可能写错"里。`get_dirs_to_reset()` 是一份白名单——但白名单里**任何一项写错**,都会照单全删。

想象未来某天,有人给 paths.py 加路径时手抖,把一个等于 `ROOT_DIR` 或 `APP_DIR` 的东西塞进了 reset 列表。会发生什么?

光想不够——**我们让它真发生一次。**

⚠️ **撞墙②会删掉整个项目根(含 `.git`)**。它会让你这一节的进度灰飞烟灭。撞墙体验不可替代,但**没必要让你把前面所有步骤重做一遍**。撞之前,做个临时快照:

🐧 Linux/macOS:
```bash
cd ..
cp -r ODPlatform ODPlatform.snapshot
cd ODPlatform
```
🪟 Windows:
```powershell
Set-Location ..; Copy-Item -Recurse ODPlatform ODPlatform.snapshot; Set-Location ODPlatform
```

打开 `paths.py`,**故意**在 `get_dirs_to_reset()` 里加一行手抖代码:

```python
def get_dirs_to_reset() -> list[Path]:
    return [
        TRAIN_DIR, VAL_DIR, TEST_DIR,
        RUNS_DIR, CHECKPOINTS_DIR, LOGGING_DIR,
        ROOT_DIR,        # ← ⚠️ 故意手抖!
    ]
```

⚡ **真撞**:执行 〔造数据〕,然后跑 v1(它会用我们刚改坏的清单):

```bash
# 〔造数据〕
python scripts/reset_project.py
```

📺 **你会看到**:

```
开始重置项目...
删除: data/train
删除: data/val
删除: data/test
删除: runs
删除: models/checkpoints
删除: apps/platform/logging
删除: .                         ← ⚠️ 整个项目根!
重置完成!
```

```bash
git status
```

📺 **你会看到**:

```
fatal: not a git repository (or any of the parent directories): .git
```

**`.git` 都没了。** 整个项目被删掉大半,`scripts/reset_project.py` 自己也再次被删。

```bash
ls -la        # 只剩零星几个文件
```

> 📌 一个细节:`shutil.rmtree(ROOT_DIR)` 大概率会**中途崩溃**——因为你的 Python 进程当前工作目录就是正在被删的目录。但崩之前,它已经把 `.git`、大量 `apps/`、`docs/` 删掉了。表现因平台而异,但**结局都是项目废了**。

### 灾难恢复

从刚才的快照恢复(干净利落,一条路):

🐧 Linux/macOS:
```bash
cd ..
rm -rf ODPlatform
cp -r ODPlatform.snapshot ODPlatform
cd ODPlatform
pip install -e ./apps/platform
```
🪟 Windows:
```powershell
Set-Location ..
Remove-Item -Recurse -Force ODPlatform
Copy-Item -Recurse ODPlatform.snapshot ODPlatform
Set-Location ODPlatform
pip install -e ./apps/platform
```

恢复后你回到"撞墙②前"的完整状态。**把那行手抖代码删掉**(从 `get_dirs_to_reset()` 移除 `ROOT_DIR`)。

> 😨 **如果你快照、远程、备份一个都没有**: 那这次就**真丢了**。这正是为什么危险操作必须有多层防护——**单点故障在生产环境就是事故。**

### 这面墙的本质:白名单也会被写错

`get_dirs_to_reset()` 把"该删什么"管住了。但它**挡不住自己被写错**。我们需要**第二道防线**:一份"绝不能删"的清单——**无论白名单里写了什么,这些目录都拒绝删除。**

## 🔧 修复: PROTECTED_DIRS + is_protected(兜底黑名单)

在 paths.py 里加一份保护清单和一个判定函数:

```python
# ============================================================
# 【绝对保护目录】reset 工具永远不能删除这些
# ============================================================
# 这是【最后一道安全网】——即使 get_dirs_to_reset() 写错了,
# reset 也会拒绝删除这些目录。
PROTECTED_DIRS: tuple[Path, ...] = (
    ROOT_DIR,                    # workspace 根 - 永远不能删
    ROOT_DIR / "apps",
    ROOT_DIR / "packages",
    APP_DIR,                     # platform 端根
    APP_DIR / "src",             # 源码目录
    SCRIPTS_DIR,                 # 进 git 的脚本
    DOCS_DIR,                    # 进 git 的文档
    UNIT_TEST_DIR,               # 进 git 的测试
    CONFIGS_DIR,                 # 进 git 的配置
    ROOT_DIR / ".git",           # git 元数据 - 删了仓库就废了
    ROOT_DIR / ".odp-workspace", # marker file
)


def is_protected(path: Path) -> bool:
    """
    路径是否受保护(任一满足即受保护):
        1. 路径本身在 PROTECTED_DIRS 中
        2. 路径是某个 PROTECTED_DIRS 成员的【祖先目录】
           (例如 ROOT_DIR 是 SCRIPTS_DIR 的祖先——删 ROOT_DIR
            就连带删了 SCRIPTS_DIR,所以删祖先也得拦)
    """
    path = path.resolve(strict=False)
    for protected in PROTECTED_DIRS:
        protected_resolved = protected.resolve(strict=False)
        if path == protected_resolved:
            return True
        # Python 3.9+ 的 is_relative_to,比 try/except ValueError 可读
        if protected_resolved.is_relative_to(path):
            return True
    return False
```

> 💡 **关键设计——双向检查。** `is_protected()` 同时拦两种情况:路径**是**受保护目录、路径**是受保护目录的祖先**。第二条尤其重要——它堵死了"删 `ROOT_DIR` 来间接删一切"这条路。这就是工业界说的**多层防御(defense in depth)**:白名单是第一层,黑名单兜底是第二层,单层失效还有下一层。

## ▶ Step 2.5: 改写 reset_project.py(v2)

```python
# apps/platform/src/od_platform/cli/reset_project.py  (v2)
import shutil
from od_platform.common.paths import ROOT_DIR, get_dirs_to_reset, is_protected


def reset_project() -> None:
    print("开始重置项目...")
    for d in get_dirs_to_reset():
        # 双重保险:即使 get_dirs_to_reset 写了危险目录,这里也拦得住
        if is_protected(d):
            print(f"⛔ 拒绝删除受保护目录: {d}")
            continue
        if d.exists():
            print(f"删除: {d.relative_to(ROOT_DIR)}")
            shutil.rmtree(d)
    print("重置完成!")


if __name__ == "__main__":
    reset_project()
```

## ▶ Step 2.6: ⚡ 真测保护——再手抖一次

这次故意写错得更狠——往 `get_dirs_to_reset()` 同时加 `ROOT_DIR` 和 `DOCS_DIR`:

```python
def get_dirs_to_reset() -> list[Path]:
    return [
        TRAIN_DIR, VAL_DIR, TEST_DIR,
        RUNS_DIR, CHECKPOINTS_DIR, LOGGING_DIR,
        ROOT_DIR,           # ← 又手抖
        DOCS_DIR,           # ← 再手抖
    ]
```

⚡ **真撞**:执行 〔造数据〕,然后跑 v2:

```bash
# 〔造数据〕
python scripts/reset_project.py
```

📺 **你会看到**——这次拦住了:

```
开始重置项目...
删除: data/train
删除: data/val
删除: data/test
删除: runs
删除: models/checkpoints
删除: apps/platform/logging
⛔ 拒绝删除受保护目录: /home/you/ODPlatform        ← ✅ 拦下来了!
⛔ 拒绝删除受保护目录: /home/you/ODPlatform/docs   ← ✅ 拦下来了!
重置完成!
```

```bash
ls docs/      # ✅ 全在
ls -d .git    # ✅ 还在
```

🎉 **双重防护工作正常。** 把那两行手抖代码去掉(从 `get_dirs_to_reset()` 删除 `ROOT_DIR` 和 `DOCS_DIR`),继续。

## 这一阶段我们做到了哪、还差什么

到这里,reset 已经**不会误删**了:白名单管住"该删什么",黑名单兜底"绝不删什么"。

但它还有个让人不安的地方——**它一上来就真删**。用户跑 `reset_project.py` 之前,**完全不知道**自己将要失去什么,只能闭着眼睛祈祷清单没写错。安全机制是给程序员的,但**用户**手上什么保障都没有。

> 📌 **v0/v1/v2 都不提交。** 我们立个判断标准,它会贯穿这一节:**"现在 checkout 这个 commit 出来,会不会让人出事?"** v2 没有任何"预演"或"确认",别人 checkout 出来跑一下就直接真删——所以还不能进 git。带 `delete` / `reset` 字样的工具,**commit 的时机本身就是安全设计的一部分**。第一个能提交的版本,要等到下一阶段加完"预演 + 确认"之后。

---

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 阶段 3 & 4: 让它先黑屏一次,再让用户看清要删什么

撞墙③(用户不放心)和撞墙④(大目录没反馈)在真实代码里几乎总是一起处理。我们先让撞墙④**亲身发生**——你才会理解为什么"进度反馈"是必需品,不是奢侈品。

## ▶ Step 4.1: ⚡ 真撞墙④——先让它真黑屏一次

⚡ **真撞**:执行 〔造数据〕(它会造 2GB + 5000 文件),然后**计时**跑 v2:

🐧 Linux/macOS:
```bash
# 〔造数据〕
time python scripts/reset_project.py
```
🪟 Windows PowerShell:
```powershell
# 〔造数据〕
Measure-Command { python scripts\reset_project.py }
```

📺 **你会看到**(用 `[T+xs]` 表示从开始算起的秒数):

```
[T+0.00s]  开始重置项目...
[T+0.01s]  删除: data/train
[T+0.02s]  删除: data/val
[T+0.03s]  删除: data/test
[T+0.04s]  删除: runs

           (此处屏幕**完全静止**,大约 5-30 秒,取决于磁盘和 CPU)

[T+18.20s] 删除: models/checkpoints
[T+18.21s] 删除: apps/platform/logging
[T+18.23s] 重置完成!

real    0m18.234s
```

> 💭 **关键体验**: 注意"删除: runs"打出来之后,屏幕**静止了 18 秒**。如果你不知道这是在删 5000 个文件 + 2GB 稀疏文件——你会以为脚本卡死了。真实场景里,**用户大概率会按 Ctrl+C**。下面就让你看看代价。

### 中途按 Ctrl+C,看"半删状态"

⚡ **真撞**:执行 〔造数据〕,跑 reset,看到 `删除: runs` 之后**立刻按 Ctrl+C**:

```bash
# 〔造数据〕
python scripts/reset_project.py
# 看到 "删除: runs" 之后,立刻按 Ctrl+C
```

按完查看:

```bash
ls runs/exp_2026_05_10/                       # best.pt 可能已删,tb_logs 还在
ls runs/exp_2026_05_10/tb_logs/ | wc -l       # 5000 个被删了一半多
```

📺 **你会看到**(数字看你按 Ctrl+C 的时机):

```
$ ls runs/exp_2026_05_10/
tb_logs/                    ← best.pt 已经被删了,但 tb_logs 还在
$ ls runs/exp_2026_05_10/tb_logs/ | wc -l
2347                        ← 5000 个文件被删了一半多,剩 2347 个
```

**这就是"半删状态"**——目录树被破坏到一个不可预测的状态。下次 reset 还能继续,但如果别的工具(比如训练脚本)以为这个目录还完整,就会出错。

清掉残骸,继续:`rm -rf runs/ data/train data/val data/test apps/platform/logging models/checkpoints`

## ▶ Step 4.2: 从这两次撞墙,推出该补什么

把刚才的痛点摊开:

- 屏幕静止十几秒、毫无反馈 → 用户以为卡死 → 按 Ctrl+C → 留下半删的烂摊子。所以 reset **删的时候必须有进度**,大目录还得提前预警"这个可能慢"。
- 再往前一步:用户跑之前,**根本不知道会删什么**。回到撞墙③那个没解决的问题——你怎么向用户**证明** `data/raw/` 是安全的?他只能猜,猜错就是几千张标注没了。所以 reset **删之前必须先把"打算删什么"摊给用户看**,让他过目、喊停。

这两条合起来,就是工业界所有危险操作的标配:**先预演(dry-run),再确认,执行时给进度**。具体落到 v3,我们让它:

- **默认就是 dry-run**——不传参数只打印计划、不真删(危险操作应当"默认安全",`git clean -n`、`terraform plan`、`kubectl --dry-run` 都是这个路子)
- 加 `--yes` 才真删;真删前还要**交互式打字 `RESET`**(全大写,制造"打字摩擦",防误按回车)
- 加 `--force` 跳过交互(给 CI / 脚本用,但仍需 `--yes`)
- 加 `--dry-run` 显式标志(默认虽是 dry-run,但显式写出来,别人读 CI 脚本一眼明白)
- 删每个目录前打印 `[i/N]` 进度,大于 1 GiB 的目录额外提示"可能要一会"

## ▶ Step 4.3: 改写 reset_project.py(v3)

替换 `apps/platform/src/od_platform/cli/reset_project.py`(完整代码):

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : reset_project.py
# @Author    : ODPlatform team
# @Project   : ODPlatform
# @Function  : 项目重置工具(v3 — dry-run + 确认 + 进度)
"""ODPlatform 项目重置工具——安全地撤销 init_project 创建的运行时产物。"""

from __future__ import annotations

import argparse
import os
import shutil
import stat
import sys
from pathlib import Path

from od_platform.common.paths import (
    ROOT_DIR,
    LOGGING_DIR,
    RAW_DATA_DIR,
    PRETRAINED_MODELS_DIR,
    get_dirs_to_reset,
    is_protected,
)
from od_platform.common.logging_utils import get_logger
from od_platform.common.string_utils import format_table_row, format_table_separator


logger = get_logger(
    base_path=LOGGING_DIR,
    log_type="reset_project",
    temp_log=False,
)


CONFIRM_KEYWORD = "RESET"
LINE_WIDTH = 70


def _format_size(bytes_size: int) -> str:
    """二进制单位(GiB/MiB/KiB)——统一用 1024 进制以避免单位混淆"""
    if bytes_size >= 1024 ** 3:
        return f"{bytes_size / (1024 ** 3):.2f} GiB"
    if bytes_size >= 1024 ** 2:
        return f"{bytes_size / (1024 ** 2):.2f} MiB"
    if bytes_size >= 1024:
        return f"{bytes_size / 1024:.2f} KiB"
    return f"{bytes_size} B"


def _on_rm_error(func, path, exc_info):
    """rmtree 错误回调——处理 Windows 只读文件场景。

    Windows 上某些被 git checkout 出来的文件会被标记只读,
    shutil.rmtree 直接 PermissionError。改成可写后重试。
    Linux 上 chmod + retry 也是无害的,所以可以跨平台用。
    """
    try:
        os.chmod(path, stat.S_IWRITE)
        func(path)
    except OSError:
        raise


def _scan_targets() -> tuple[list[tuple[Path, int, int]], list[Path]]:
    """扫描所有目标,返回 (可删除的, 跳过的)。
    可删除项是 (path, file_count, total_size_bytes) 三元组。
    """
    deletable: list[tuple[Path, int, int]] = []
    skipped: list[Path] = []

    for d in get_dirs_to_reset():
        if is_protected(d):
            logger.warning(f"⛔ 拒绝处理受保护目录(配置可能有误): {d}")
            skipped.append(d)
            continue
        if not d.exists():
            skipped.append(d)
            continue

        file_count = 0
        total_size = 0
        try:
            for f in d.rglob("*"):
                if f.is_file():
                    file_count += 1
                    try:
                        total_size += f.stat().st_size
                    except OSError:
                        pass
        except OSError as e:
            logger.warning(f"扫描 {d} 时出错: {e}")

        deletable.append((d, file_count, total_size))

    return deletable, skipped


def _print_plan(
    deletable: list[tuple[Path, int, int]],
    skipped: list[Path],
    will_actually_delete: bool,
) -> None:
    if will_actually_delete:
        logger.warning("⚠️  即将删除以下目录".center(LINE_WIDTH, '='))
    else:
        logger.info("📋 [DRY-RUN] 计划如下(未实际删除)".center(LINE_WIDTH, '='))

    if not deletable:
        logger.info("(没有可删除的目录——项目已经是干净状态)")
        return

    widths = [40, 12, 14]
    aligns = ['left', 'right', 'right']
    logger.info(format_table_row(['目录', '文件数', '大小'], widths, aligns))
    logger.info(format_table_separator(widths))

    total_files = 0
    total_bytes = 0
    for path, count, size in deletable:
        rel = path.relative_to(ROOT_DIR)
        logger.info(format_table_row(
            [str(rel), str(count), _format_size(size)], widths, aligns,
        ))
        total_files += count
        total_bytes += size

    logger.info(format_table_separator(widths))
    logger.info(format_table_row(
        ['【合计】', str(total_files), _format_size(total_bytes)], widths, aligns,
    ))

    logger.info("")
    logger.info("✅ 以下重要目录【不会】被动:")
    logger.info(f"  - 原始数据: {RAW_DATA_DIR.relative_to(ROOT_DIR)}/")
    logger.info(f"  - 预训练权重: {PRETRAINED_MODELS_DIR.relative_to(ROOT_DIR)}/")
    logger.info(f"  - 所有代码、文档、配置(进 git 的)")


def _confirm(deletable_count: int) -> bool:
    """交互式确认。

    用 print 而非 logger,这是【刻意的视觉打断】——
    让用户的眼睛从"扫日志"模式切换到"主动决策"模式,
    避免误按回车造成不可逆操作。这是工业级危险操作的标准设计。
    """
    print()
    print("=" * LINE_WIDTH)
    print(f"⚠️  你正要删除 {deletable_count} 个目录的内容。这个操作不可撤销。")
    print(f"⚠️  如果确认,请精确输入大写的 '{CONFIRM_KEYWORD}'(其他任何输入都会取消):")
    print("=" * LINE_WIDTH)
    try:
        user_input = input("> ").strip()
    except (KeyboardInterrupt, EOFError):
        print()
        return False
    return user_input == CONFIRM_KEYWORD


def _delete_one(path: Path, idx: int, total: int, file_count: int, size: int) -> str | None:
    """删除单个目录,带进度提示。返回 None=成功,字符串=失败原因"""
    if is_protected(path):
        logger.error(f"[{idx}/{total}] ⛔ 删除前检查失败,跳过: {path}")
        return "受保护目录"

    rel = path.relative_to(ROOT_DIR)
    size_str = _format_size(size)

    if size > 1024 ** 3:  # > 1 GiB
        logger.warning(
            f"[{idx}/{total}] 正在删除 {rel} ({size_str}, {file_count} 个文件)"
            f"——这可能需要一会..."
        )
    else:
        logger.info(f"[{idx}/{total}] 删除 {rel} ({size_str}, {file_count} 个文件)")

    try:
        shutil.rmtree(path, onerror=_on_rm_error)
        logger.info(f"[{idx}/{total}] ✅ 已删除: {rel}")
        return None
    except OSError as e:
        logger.error(f"[{idx}/{total}] ❌ 删除失败 {rel}: {e}")
        return str(e)


def _execute_delete(deletable: list[tuple[Path, int, int]]) -> None:
    total = len(deletable)
    success: list[Path] = []
    failed: list[tuple[Path, str]] = []

    for idx, (path, file_count, size) in enumerate(deletable, 1):
        reason = _delete_one(path, idx, total, file_count, size)
        if reason is None:
            success.append(path)
        else:
            failed.append((path, reason))

    logger.info("=" * LINE_WIDTH)
    if failed:
        logger.warning(f"完成: 成功 {len(success)} 个,失败 {len(failed)} 个")
        for p, reason in failed:
            logger.warning(f"  - {p.relative_to(ROOT_DIR)}: {reason}")
    else:
        logger.info(f"完成: 成功 {len(success)} 个,失败 0 个")


def reset_project(yes: bool = False, force: bool = False, dry_run: bool = False) -> int:
    """
    Args:
        yes:     --yes,显式同意删除
        force:   --force,跳过交互式确认
        dry_run: --dry-run,显式声明只打印不删除(默认行为也是这个)

    --dry-run 与 --yes 互斥;同时给以 --dry-run 优先并警告。
    """
    logger.info("项目重置工具".center(LINE_WIDTH, '='))
    logger.info(f"项目根目录: {ROOT_DIR}")

    if dry_run and yes:
        logger.warning("⚠️  同时给了 --dry-run 和 --yes,以 --dry-run 为准(只打印不删除)")
        yes = False

    deletable, skipped = _scan_targets()
    _print_plan(deletable, skipped, will_actually_delete=yes)

    if not deletable:
        return 0

    if not yes:
        logger.info("")
        if dry_run:
            logger.info("💡 这是显式的 --dry-run。要真正执行删除,请加 --yes:")
        else:
            logger.info("💡 这是 dry-run(默认行为)。要真正执行删除,请加 --yes:")
        logger.info("   python scripts/reset_project.py --yes")
        return 0

    if not force:
        if not _confirm(len(deletable)):
            logger.warning("❌ 用户取消,未执行删除")
            return 1

    logger.info("")
    logger.info("开始删除...".center(LINE_WIDTH, '='))
    _execute_delete(deletable)
    return 0


def main() -> int:
    parser = argparse.ArgumentParser(
        description="重置 ODPlatform 项目——撤销 init_project 创建的运行时产物。",
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument("--yes", action="store_true", help="真正执行删除(默认是 dry-run)")
    parser.add_argument("--force", action="store_true", help="跳过交互式确认(仅当 --yes 时有效)")
    parser.add_argument("--dry-run", action="store_true", help="显式声明 dry-run")
    args = parser.parse_args()
    return reset_project(yes=args.yes, force=args.force, dry_run=args.dry_run)


if __name__ == "__main__":
    sys.exit(main())
```

> 💡 几处比朴素版关键的工程改进:`_format_size` 用 GiB/MiB(1024 进制配二进制单位);`_on_rm_error` 处理 Windows 只读文件;`is_protected` 在扫描和删除前各查一次(双保险);进度 `[i/N]` + 大目录预警。

## ▶ Step 4.4: ⚠️ 必做——同步更新入口脚本

1.1 节的 `scripts/reset_project.py` 是为 v0 写的,直接调了**无参的** `reset_project()`。但 v3 的 `reset_project(yes, force, dry_run)` 要靠 `main()` 解析 argparse 再把参数传进来。如果入口还在裸调 `reset_project()`,argparse 永远不执行,所有 flag 都是默认值——

> **现象**: 你跑 `--yes`,看到的却是 dry-run 输出,误以为 `--yes` 没生效。

改成调 `main()`:

```python
# scripts/reset_project.py(v3 起的正确写法)
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "apps" / "platform" / "src"))

from od_platform.cli.reset_project import main      # ← 改成 import main

if __name__ == "__main__":
    sys.exit(main())                                  # ← 改成 sys.exit(main())
```

> 💭 **教训**: 重构 cli 函数签名时,**入口脚本是隐藏的调用方**。Python 没有编译期检查告诉你"调用点没同步更新",只有运行期默默拿默认值。这就是为什么"入口超薄(只 import + 调 main)"是值得坚持的纪律——薄到没改动,就不会忘记同步。

## ▶ Step 4.5: ⚡ 真测三种模式

### 模式 1:dry-run(默认)

```bash
# 〔造数据〕
python scripts/reset_project.py
```

📺 **你会看到**(很快就出来了,因为没真删,只在扫描):

```
============================ 项目重置工具 ============================
项目根目录: /xxx/ODPlatform
==================== 📋 [DRY-RUN] 计划如下(未实际删除) ====================
目录                                          文件数             大小
----------------------------------------------------------------------
data/train                                         0              0 B
data/val                                           0              0 B
data/test                                          0              0 B
runs                                            5001          2.00 GiB
models/checkpoints                                 0              0 B
apps/platform/logging                             50         <1 KiB
----------------------------------------------------------------------
【合计】                                        5051          2.00 GiB

✅ 以下重要目录【不会】被动:
  - 原始数据: data/raw/
  - 预训练权重: models/pretrained/
  - 所有代码、文档、配置(进 git 的)

💡 这是 dry-run(默认行为)。要真正执行删除,请加 --yes:
   python scripts/reset_project.py --yes
```

**用户现在能看清要删什么了**——撞墙③解决。

### 模式 2:--yes(带交互确认)

```bash
# 〔造数据〕
python scripts/reset_project.py --yes
```

会先打印计划,然后停在确认那里要你打字。输入小写 `reset`、空字符串、或直接 Ctrl+C,都会取消;只有精确输入大写 `RESET` 才执行。执行时你会看到 `[1/6] [2/6]...` 的进度,`runs` 那个 2GB 的会额外提示"这可能需要一会"。**撞墙④解决。**

> 🪟 **Windows 用户注意**: 这一步删到最后一个 `apps/platform/logging` 时,你大概率会撞到一个 `[WinError 32] 另一个程序正在使用此文件`。**先把这个现象记下来,别急着修**——它不是 Windows 在搞特殊,而是一个真实的设计 bug,我们下一阶段(撞墙⑤)正式拆它。

### 模式 3:--yes --force(无交互,给 CI / 脚本)

```bash
# 〔造数据〕
python scripts/reset_project.py --yes --force
```

跳过打字确认,直接执行。

## ▶ Step 4.6: ⭐ 第一次提交(v3 是第一个安全可发布的版本)

到现在为止,我们一个字都没提交过——因为 v0/v1/v2 任何人 checkout 出来跑一下都可能出事。**v3 不一样:默认 dry-run,不会有人不小心真删。** 这是第一个"checkout 出来不会让人出事"的版本,可以进 git 了。

这次要提交两样东西:paths.py 里我们在阶段 2 加的 reset 清单 + 双重防护(一直没提交),和 v3 本体。分两个 commit:

```bash
# Commit 1: paths.py 的 reset 清单 + 保护机制
git add apps/platform/src/od_platform/common/paths.py
git commit -m "feat(common): add reset target list + protection mechanism in paths.py"

# Commit 2: reset_project v3 + 入口
git add apps/platform/src/od_platform/cli/reset_project.py scripts/reset_project.py
git commit -m "feat(cli): add reset_project.py with dry-run + confirmation + progress"

git push origin main
```

🎉 **真正安全的 reset 工具雏形完成。**

但是……(还记得 Windows 那个 `WinError 32` 吗?)

---

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 阶段 5: 撞墙⑤——工具把自己的日志删了

## ▶ Step 5.1: ⚡ 真撞墙⑤(Windows)——亲眼看到 WinError 32

> 🪟 Windows 用户:这就是你 4.5 模式 2 撞到的 `WinError 32`。现在我们正式分析它。
> 🐧 只有 Linux/macOS?**也别跳过**——5.2 节会在 Linux 上把这个 bug 用 `lsof` 可视化,POSIX 语义那一版更深刻。但如果你手边有 Windows,优先在 Windows 撞一次,错误信息直白得多。

⚡ **真撞**(Windows PowerShell):

```powershell
# 〔造数据〕
python scripts\reset_project.py --yes --force
```

📺 **你会看到**(前几个目录正常,最后一个 `apps/platform/logging` 报错):

```
================ 开始删除... ================
[1/6] 删除 data/train (0 B, 0 个文件)
[1/6] ✅ 已删除: data/train
... (省略 2~5)
[6/6] 删除 apps/platform/logging (<1 KiB, 50 个文件)
[6/6] ❌ 删除失败 apps\platform\logging:
[WinError 32] 另一个程序正在使用此文件,进程无法访问。:
'C:\\...\\apps\\platform\\logging\\reset_project\\reset-project_20260510-020450.log'
======================================================================
完成: 成功 5 个,失败 1 个
```

第一反应通常是:"Windows 又在搞特殊?换 Linux 跑就好了。"

**千万别这么以为**——这是一个真实的设计 bug,Windows 只是把它**显式报了出来**。Linux 上同样的代码会怎样?**rmtree 成功,然后后续日志静默消失**——更危险。下面就让你**亲眼看到**这个静默丢失。

## ▶ Step 5.2: ⚡ 真撞墙⑤(Linux/macOS)——亲眼看见 (deleted) 标记

在 Linux 上,POSIX 文件语义会让 bug "看起来不存在"——但日志其实已经凭空消失。我们用 `lsof` 和 `/proc/<pid>/fd/` 把这个看不见的 bug 可视化。

**用编辑器**创建 `scripts/demo_orphan_inode.py`:

```python
#!/usr/bin/env python
"""
撞墙⑤ Linux/macOS 演示:孤儿 inode 静默丢失。

跑这个脚本,然后【在另一个终端】观察 fd 状态变化。
你会亲眼看到日志文件被删除后,我们的 fd 仍然有效——
但写入的内容飞进黑洞。

仅 Linux/macOS,Windows 会在 step 2 的 rmtree 处提前 WinError 32。
"""
import os
import shutil
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
LOGGING_DIR = REPO_ROOT / "apps" / "platform" / "logging"


def main() -> None:
    LOGGING_DIR.mkdir(parents=True, exist_ok=True)
    log_path = LOGGING_DIR / "demo_orphan.log"

    # Step 1: 打开日志文件,持有 fd
    fd = open(log_path, "w")
    fd.write("step 1: log opened OK\n")
    fd.flush()

    pid = os.getpid()
    fd_no = fd.fileno()

    print(f"\n{'=' * 60}")
    print(f"PID = {pid}")
    print(f"日志文件: {log_path}")
    print(f"FD: {fd_no}")
    print(f"{'=' * 60}")
    print(f"\n👉 现在【另开一个终端】,跑下面命令(任选一条):")
    print(f"   # Linux(推荐 — /proc 直接看 fd 软链)")
    print(f"   ls -l /proc/{pid}/fd/{fd_no}")
    print(f"   # 跨平台(Linux/macOS 都行 — lsof 通用)")
    print(f"   lsof -p {pid} | grep demo_orphan")
    input("\n按回车继续(进入 step 2: 开始 rmtree)...")

    # Step 2: rmtree LOGGING_DIR(自指 bug 触发点)
    print(f"\n>>> 即将 shutil.rmtree({LOGGING_DIR})...")
    shutil.rmtree(LOGGING_DIR)
    print(f">>> rmtree 完成。Linux/macOS 上没有报错!这就是 bug 的隐蔽之处。")

    # Step 3: 在另一个终端验证孤儿 inode
    print(f"\n👉 现在【在另一个终端】再跑一次相同命令:")
    print(f"   # Linux:  ls -l /proc/{pid}/fd/{fd_no}")
    print(f"   # macOS:  lsof -p {pid} | grep demo_orphan")
    print(f"你应该看到软链路径后面多了 '(deleted)' 标记。")
    input("\n看完按回车继续(进入 step 4: 写入孤儿 fd)...")

    # Step 4: 后续写入静默丢失
    fd.write("step 2: 这条日志看起来写成功了,但是孤儿 inode\n")
    fd.flush()
    print(f"\n>>> fd.write() 没报错(write() syscall 在孤儿 fd 上完全合法)")
    print(f">>> 但是 cat {log_path}  ← 文件不存在")
    print(f">>> 进程退出后,孤儿 inode 被释放,数据【凭空消失】")
    print(f"\nWindows 在 step 2 的 rmtree 那一刻就 WinError 32——直接砸你脸上")
    print(f"Linux/macOS 不报,然后日志凭空消失——更要命")

    fd.close()


if __name__ == "__main__":
    main()
```

⚡ **真撞**(开两个终端):

**Terminal A**:
```bash
python scripts/demo_orphan_inode.py
```

脚本会打印它的 PID 和 FD,然后停在第一个回车。

**Terminal B**(rmtree 之前先看一次):
```bash
ls -l /proc/<PID>/fd/<FD>        # macOS 用: lsof -p <PID> | grep demo_orphan
```

📺 **Terminal B 你会看到**(正常的 fd 软链,指向真实路径):

```
lrwx------ 1 you you 64 ... /proc/123456/fd/3 -> /home/you/ODPlatform/apps/platform/logging/demo_orphan.log
```

回 **Terminal A** 按回车,让 `rmtree` 跑掉。然后回 **Terminal B**,**再跑一次同样的 ls**:

📺 **Terminal B 你会看到**(`(deleted)` 出现了!):

```
lrwx------ 1 you you 64 ... /proc/123456/fd/3 -> '/home/you/ODPlatform/apps/platform/logging/demo_orphan.log (deleted)'
```

**`(deleted)` 标记出现了——这就是孤儿 inode。** 文件名(目录项)已经从文件系统删除,但 inode 因为还有 fd 持有,数据块还在。回 Terminal A 按回车让脚本写入并退出,再 `ls` 那个日志文件:

```
ls: cannot access '.../demo_orphan.log': No such file or directory
```

**写入操作完全成功,但是没人能看到——日志凭空消失了。**

## ▶ Step 5.3: 这次撞墙跟前 4 次有本质区别

| 撞墙 | 危险针对谁 |
|---|---|
| ① init/reset 不对称 | 用户的代码、文档 |
| ② 白名单可能写错 | 用户的整个 workspace |
| ③ 用户不知会发生什么 | 用户的原始数据 |
| ④ 大数据集没反馈 | 用户的耐心 |
| **⑤ ←这次** | **工具自己** |

**前 4 次都是"工具对外部世界的危险",这一次是"工具对自己的危险"——工具的运行时状态污染了它要清理的目标。**

## ▶ Step 5.4: 问题根源——reset 的自指依赖

看 v3 是怎么配 logger 的:

```python
logger = get_logger(base_path=LOGGING_DIR, log_type="reset_project")
```

它的日志文件落在 `apps/platform/logging/reset_project/...log`。而 `LOGGING_DIR` 是哪个?**正是 `get_dirs_to_reset()` 清单里的成员之一**——reset 自己要删的目标。整个矛盾:

```
reset 启动 → 打开日志文件(进程持有写句柄 fd)
          → 循环删除到 LOGGING_DIR 这一项
          → shutil.rmtree(LOGGING_DIR)
              💥 Windows: 文件被自己进程占着,删不掉 → WinError 32
              💥 Linux:   底层 unlink 成功,目录没了,
                          但 fd 还指向那个已成孤儿的 inode,
                          后续 logger.info(...) 写入 → 黑洞 👻
```

> 💡 **金句**: **"Windows 把这个 bug 砸你脸上,Linux 让你蒙在鼓里——前者烦人,后者要命。"**

这种"工具自己消费自己要管理的资源",工程界叫**元工具自指问题(meta-tool self-reference)**,场景比你想的多:数据库迁移工具的迁移日志表被该次迁移修改;备份程序的临时文件占用要备份的空间;logrotate 自己写的日志要被它轮转;`apt` 升级 `apt` 自己时要换二进制……我们这里是 reset 把日志写进了自己要删的目录。

## ▶ Step 5.5: 为什么"把日志挪出去"才是正解——审计追踪原则

直觉上你可能想用最小改动修它:reset 启动后把 file handler 摘掉、把空日志删掉。能修,但**违反企业惯例**:清理工具不该删自己的日志。看真实世界:

| 工具 | 自己的日志 | 处理方式 |
|---|---|---|
| Ansible cleanup playbooks | `/var/log/ansible.log` | **不删** |
| Terraform destroy | `terraform.log` | **不删** |
| Alembic / Flyway 数据库回滚 | 迁移历史表 | **永远不删** |
| `apt autoremove` | `/var/log/apt/history.log` | **不删** |

模式高度统一:**清理工具可以不写日志,但一旦写了,就永不自删。** 为什么?

1. **审计追踪**:reset 是破坏性操作,出事时(同事抱怨"我数据没了")**第一份证据就是日志**。日志被自己删,成了无头案。SOX / HIPAA / ISO 27001 都关心这个。
2. **调试可能性**:reset 半路崩了,留下半个被删的目录树——这时候最有价值的就是那截日志。
3. **用户安心感**:用户能在某处看到"我做过什么"的清单,才敢用这个工具。

> 💡 **金句**: **"销毁操作的记录,不能被该操作销毁。这是工业级清理工具的第一原则。"**

所以正解不是"删自己的日志",而是**把 reset 的日志写到一个 reset 永远不碰的地方**——一个独立的"工具元数据"命名空间,跟业务日志严格分开。

## ▶ Step 5.6: paths.py 加 META_DIR

```
ROOT_DIR/
├── apps/platform/logging/   ← 业务日志(LOGGING_DIR):train/val 等业务工具写这里,会被 reset 清理
└── .odp-meta/               ← 工具元数据(META_DIR)
    └── logs/                ← 元工具日志(META_LOGGING_DIR):reset 自己的审计日志,永不被 reset 清理
```

在 paths.py 加常量、加进 `get_dirs_to_initialize()`、加进 `PROTECTED_DIRS`,**绝对不加进** `get_dirs_to_reset()`:

```python
# ============================================================
# 【工具元数据目录】(audit trail / 工具自身日志)
# ============================================================
# 业务工具(train/val/inference)的日志 → LOGGING_DIR,会被 reset 清理
# 元工具(reset/doctor/upgrade)的日志 → META_LOGGING_DIR,是对清理动作本身的
# 审计追踪——如果存 LOGGING_DIR 里会被它们自己清掉,违背审计本意。
META_DIR: Path = ROOT_DIR / ".odp-meta"
META_LOGGING_DIR: Path = META_DIR / "logs"


def get_dirs_to_initialize() -> list[Path]:
    return [
        # ... (原有目录)
        META_LOGGING_DIR,        # 工具元数据(reset 等元工具的审计日志位置)
    ]


# get_dirs_to_reset() 保持不变 —— META_LOGGING_DIR 【绝对不在】这里


PROTECTED_DIRS: tuple[Path, ...] = (
    # ... (原有保护目录)
    META_DIR,            # 工具元数据根
    META_LOGGING_DIR,    # 工具自身日志 - 审计追踪不可被破坏
)
```

`.gitignore` 加一行(本地运行历史,不进版本控制):

```gitignore
# 工具元数据(reset/doctor 等元工具的审计日志,本地运行历史)
.odp-meta/
```

## ▶ Step 5.7: reset_project 升级到 v4

从 v3 到 v4,改动很聚焦——只动 5 处:① import 把 `LOGGING_DIR` 换成 `META_LOGGING_DIR`;② logger 配置的 `base_path` 跟着换;③ 多 `import getpass / subprocess`;④ 加一个 `_audit_context()`;⑤ `reset_project()` 开头记一条审计上下文。

> 💡 **为什么 `_audit_context()` 不是"可选"**: "日志挪到 reset 删不到的地方"是审计追踪的**外部条件**(日志活下来);而"日志里真的写了能查出谁干的的信息"是**内部条件**。光有位置不够,内容也得有审计价值——所以这两件事是一对原则的两面,一起做。

下面是整合后的 v4 完整文件,直接覆盖 `apps/platform/src/od_platform/cli/reset_project.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : reset_project.py
# @Author    : ODPlatform team
# @Project   : ODPlatform
# @Function  : 项目重置工具(v4 — META_LOGGING_DIR + 审计上下文)
"""ODPlatform 项目重置工具——安全地撤销 init_project 创建的运行时产物。

v4 设计要点:
    1. 默认 dry-run(显式 --yes 才执行)
    2. 必须打字 'RESET' 才删除(--force 跳过交互)
    3. 双重防护:get_dirs_to_reset 白名单 + is_protected 黑名单
    4. 进度反馈 + 大目录预警
    5. Windows 只读文件兼容(_on_rm_error)
    6. 日志写在 META_LOGGING_DIR,不在自己要删的目录(撞墙⑤教训)
    7. 审计上下文(user / git_rev / argv / cwd / pid)随每次运行记录
"""

from __future__ import annotations

import argparse
import getpass
import os
import shutil
import stat
import subprocess
import sys
from pathlib import Path

from od_platform.common.paths import (
    ROOT_DIR,
    META_LOGGING_DIR,             # ← v4: 不再是 LOGGING_DIR
    RAW_DATA_DIR,
    PRETRAINED_MODELS_DIR,
    get_dirs_to_reset,
    is_protected,
)
from od_platform.common.logging_utils import get_logger
from od_platform.common.string_utils import format_table_row, format_table_separator


logger = get_logger(
    base_path=META_LOGGING_DIR,   # ← v4: 写到 .odp-meta/logs/
    log_type="reset_project",
    temp_log=False,
)


CONFIRM_KEYWORD = "RESET"
LINE_WIDTH = 70


# ─────────────────────────────────────────────────────────
# 工具函数
# ─────────────────────────────────────────────────────────

def _format_size(bytes_size: int) -> str:
    """二进制单位(GiB/MiB/KiB)——统一用 1024 进制以避免单位混淆"""
    if bytes_size >= 1024 ** 3:
        return f"{bytes_size / (1024 ** 3):.2f} GiB"
    if bytes_size >= 1024 ** 2:
        return f"{bytes_size / (1024 ** 2):.2f} MiB"
    if bytes_size >= 1024:
        return f"{bytes_size / 1024:.2f} KiB"
    return f"{bytes_size} B"


def _on_rm_error(func, path, exc_info):
    """rmtree 错误回调——处理 Windows 只读文件场景。

    Windows 上某些被 git checkout 出来的文件会被标记只读,
    shutil.rmtree 直接 PermissionError。改成可写后重试。
    Linux 上 chmod + retry 也是无害的,所以可以跨平台用。
    """
    try:
        os.chmod(path, stat.S_IWRITE)
        func(path)
    except OSError:
        raise


def _audit_context() -> dict:
    """收集审计上下文——user / git_rev / argv / cwd / pid。

    为什么要这个?
        - 出事时("我的 raw 数据没了!"),审计日志是第一份证据
        - 单纯一行"reset 完成"不够——必须能查到【谁】在【哪个版本的代码】上跑的
        - SOX / HIPAA / ISO 27001 等合规要求里都有类似条款
    """
    try:
        git_rev = subprocess.check_output(
            ["git", "rev-parse", "HEAD"], cwd=ROOT_DIR, text=True,
            stderr=subprocess.DEVNULL,
        ).strip()
    except (subprocess.CalledProcessError, FileNotFoundError):
        git_rev = "(not a git repo)"
    return {
        "user": getpass.getuser(),
        "pid": os.getpid(),
        "git_rev": git_rev,
        "argv": sys.argv,
        "cwd": os.getcwd(),
    }


# ─────────────────────────────────────────────────────────
# 扫描 / 计划展示 / 确认 / 删除
# ─────────────────────────────────────────────────────────

def _scan_targets() -> tuple[list[tuple[Path, int, int]], list[Path]]:
    """扫描所有目标,返回 (可删除的, 跳过的)。
    可删除项是 (path, file_count, total_size_bytes) 三元组。
    """
    deletable: list[tuple[Path, int, int]] = []
    skipped: list[Path] = []

    for d in get_dirs_to_reset():
        if is_protected(d):
            logger.warning(f"⛔ 拒绝处理受保护目录(配置可能有误): {d}")
            skipped.append(d)
            continue
        if not d.exists():
            skipped.append(d)
            continue

        file_count = 0
        total_size = 0
        try:
            for f in d.rglob("*"):
                if f.is_file():
                    file_count += 1
                    try:
                        total_size += f.stat().st_size
                    except OSError:
                        pass
        except OSError as e:
            logger.warning(f"扫描 {d} 时出错: {e}")

        deletable.append((d, file_count, total_size))

    return deletable, skipped


def _print_plan(
    deletable: list[tuple[Path, int, int]],
    skipped: list[Path],
    will_actually_delete: bool,
) -> None:
    if will_actually_delete:
        logger.warning("⚠️  即将删除以下目录".center(LINE_WIDTH, '='))
    else:
        logger.info("📋 [DRY-RUN] 计划如下(未实际删除)".center(LINE_WIDTH, '='))

    if not deletable:
        logger.info("(没有可删除的目录——项目已经是干净状态)")
        return

    widths = [40, 12, 14]
    aligns = ['left', 'right', 'right']
    logger.info(format_table_row(['目录', '文件数', '大小'], widths, aligns))
    logger.info(format_table_separator(widths))

    total_files = 0
    total_bytes = 0
    for path, count, size in deletable:
        rel = path.relative_to(ROOT_DIR)
        logger.info(format_table_row(
            [str(rel), str(count), _format_size(size)], widths, aligns,
        ))
        total_files += count
        total_bytes += size

    logger.info(format_table_separator(widths))
    logger.info(format_table_row(
        ['【合计】', str(total_files), _format_size(total_bytes)], widths, aligns,
    ))

    logger.info("")
    logger.info("✅ 以下重要目录【不会】被动:")
    logger.info(f"  - 原始数据: {RAW_DATA_DIR.relative_to(ROOT_DIR)}/")
    logger.info(f"  - 预训练权重: {PRETRAINED_MODELS_DIR.relative_to(ROOT_DIR)}/")
    logger.info(f"  - 所有代码、文档、配置(进 git 的)")


def _confirm(deletable_count: int) -> bool:
    """交互式确认。

    用 print 而非 logger,这是【刻意的视觉打断】——
    让用户的眼睛从"扫日志"模式切换到"主动决策"模式,
    避免误按回车造成不可逆操作。这是工业级危险操作的标准设计。
    """
    print()
    print("=" * LINE_WIDTH)
    print(f"⚠️  你正要删除 {deletable_count} 个目录的内容。这个操作不可撤销。")
    print(f"⚠️  如果确认,请精确输入大写的 '{CONFIRM_KEYWORD}'(其他任何输入都会取消):")
    print("=" * LINE_WIDTH)
    try:
        user_input = input("> ").strip()
    except (KeyboardInterrupt, EOFError):
        print()
        return False
    return user_input == CONFIRM_KEYWORD


def _delete_one(path: Path, idx: int, total: int, file_count: int, size: int) -> str | None:
    """删除单个目录,带进度提示。返回 None=成功,字符串=失败原因"""
    if is_protected(path):
        logger.error(f"[{idx}/{total}] ⛔ 删除前检查失败,跳过: {path}")
        return "受保护目录"

    rel = path.relative_to(ROOT_DIR)
    size_str = _format_size(size)

    if size > 1024 ** 3:  # > 1 GiB
        logger.warning(
            f"[{idx}/{total}] 正在删除 {rel} ({size_str}, {file_count} 个文件)"
            f"——这可能需要一会..."
        )
    else:
        logger.info(f"[{idx}/{total}] 删除 {rel} ({size_str}, {file_count} 个文件)")

    try:
        shutil.rmtree(path, onerror=_on_rm_error)
        logger.info(f"[{idx}/{total}] ✅ 已删除: {rel}")
        return None
    except OSError as e:
        logger.error(f"[{idx}/{total}] ❌ 删除失败 {rel}: {e}")
        return str(e)


def _execute_delete(deletable: list[tuple[Path, int, int]]) -> None:
    total = len(deletable)
    success: list[Path] = []
    failed: list[tuple[Path, str]] = []

    for idx, (path, file_count, size) in enumerate(deletable, 1):
        reason = _delete_one(path, idx, total, file_count, size)
        if reason is None:
            success.append(path)
        else:
            failed.append((path, reason))

    logger.info("=" * LINE_WIDTH)
    if failed:
        logger.warning(f"完成: 成功 {len(success)} 个,失败 {len(failed)} 个")
        for p, reason in failed:
            logger.warning(f"  - {p.relative_to(ROOT_DIR)}: {reason}")
    else:
        logger.info(f"完成: 成功 {len(success)} 个,失败 0 个")


# ─────────────────────────────────────────────────────────
# 主流程
# ─────────────────────────────────────────────────────────

def reset_project(yes: bool = False, force: bool = False, dry_run: bool = False) -> int:
    """
    --dry-run 与 --yes 互斥;同时给以 --dry-run 优先并警告。
    """
    logger.info("项目重置工具".center(LINE_WIDTH, '='))
    logger.info(f"项目根目录: {ROOT_DIR}")

    # 审计上下文——出事时第一份证据,写进(永不自删的)审计日志
    ctx = _audit_context()
    logger.info(f"审计: user={ctx['user']}  pid={ctx['pid']}  git={ctx['git_rev']}")
    logger.info(f"审计: cwd={ctx['cwd']}")
    logger.info(f"审计: argv={' '.join(ctx['argv'])}")

    if dry_run and yes:
        logger.warning("⚠️  同时给了 --dry-run 和 --yes,以 --dry-run 为准(只打印不删除)")
        yes = False

    deletable, skipped = _scan_targets()
    _print_plan(deletable, skipped, will_actually_delete=yes)

    if not deletable:
        return 0

    if not yes:
        logger.info("")
        if dry_run:
            logger.info("💡 这是显式的 --dry-run。要真正执行删除,请加 --yes:")
        else:
            logger.info("💡 这是 dry-run(默认行为)。要真正执行删除,请加 --yes:")
        logger.info("   odp-reset --yes")
        return 0

    if not force:
        if not _confirm(len(deletable)):
            logger.warning("❌ 用户取消,未执行删除")
            return 1

    logger.info("")
    logger.info("开始删除...".center(LINE_WIDTH, '='))
    _execute_delete(deletable)
    return 0


def main() -> int:
    parser = argparse.ArgumentParser(
        description="重置 ODPlatform 项目——撤销 init_project 创建的运行时产物。",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog="""
示例:
  odp-reset                      # 默认 dry-run(推荐先跑这个)
  odp-reset --yes                # 真删除,带交互式确认
  odp-reset --yes --force        # CI / 脚本调用,跳过交互
  odp-reset --dry-run            # 显式 dry-run

注意:
  - 永远不删除 data/raw/(原始数据)
  - 永远不删除代码、文档、配置(任何进 git 的目录)
  - 审计日志写在 .odp-meta/logs/reset_project/,不会被自身清理
""",
    )
    parser.add_argument("--yes", action="store_true", help="真正执行删除(默认是 dry-run)")
    parser.add_argument("--force", action="store_true", help="跳过交互式确认(仅当 --yes 时有效)")
    parser.add_argument("--dry-run", action="store_true", help="显式声明 dry-run")
    args = parser.parse_args()
    return reset_project(yes=args.yes, force=args.force, dry_run=args.dry_run)


if __name__ == "__main__":
    sys.exit(main())
```

## ▶ Step 5.8: ⚡ 真验证修复

⚡ **真撞**(尤其在 Windows 上——上次的 WinError 32 应该消失):

🐧 Linux/macOS:
```bash
# 〔造数据〕之前,先 odp-init 一次,顺带建好 .odp-meta/logs/
rm -rf data/raw/precious_dataset data/train data/val data/test \
       runs models/checkpoints apps/platform/logging
odp-init
./scripts/make_disaster_data.sh
python scripts/reset_project.py --yes --force
```
🪟 Windows:
```powershell
Remove-Item -Recurse -Force -ErrorAction SilentlyContinue `
    data/raw/precious_dataset, data/train, data/val, data/test, `
    runs, models/checkpoints, apps/platform/logging
odp-init
.\scripts\make_disaster_data.ps1
python scripts\reset_project.py --yes --force
```

📺 **你会看到**(Windows 上不再 WinError 32!):

```
================ 开始删除... ================
[1/6] 删除 data/train (0 B, 0 个文件)
[1/6] ✅ 已删除: data/train
... (省略 2~5)
[6/6] 删除 apps/platform/logging (<1 KiB, 50 个文件)
[6/6] ✅ 已删除: apps/platform/logging          ← ✅ 这次成功了!
======================================================================
完成: 成功 6 个,失败 0 个
```

确认审计日志**活了下来**,而且查得到是谁干的:

```bash
ls .odp-meta/logs/reset_project/
cat .odp-meta/logs/reset_project/*.log | grep 审计
```

📺 你会看到审计行里有 `user` / `git` / `argv`——**reset 操作的审计追踪存活了**。再确认业务日志被清理:`ls apps/platform/logging/`(不存在或为空)。

bug 根除,Windows / Linux 行为一致,而且日志作为审计追踪持久保留。

## ▶ Step 5.9: 撞墙⑤的真正教训

| 撞墙 | 修复方式 |
|---|---|
| ① 不对称 | **加** 白名单 `get_dirs_to_reset` |
| ② 名单可错 | **加** 黑名单 `is_protected` |
| ③ 不知会发生什么 | **加** dry-run + 确认 |
| ④ 大操作没反馈 | **加** 进度条 |
| **⑤ 工具污染自己** | **重新分层**:把日志移到独立命名空间 |

前 4 次都是"加防御"。第 5 次是**"重新看清责任边界"**——业务日志和元工具日志,本就是两类东西,只是早期没意识到要分开。

> 💡 **金句**: **"看清 bug 真正在哪一刀:有时不是代码写错了,而是【架构里少了一层】。"** 工程师成长的关键转折,就是从"看到 bug 就改代码"变成"看到 bug 先问:是不是少了一个抽象?"

## ▶ Step 5.10: ⭐ 提交修复(3 个文件,分开 commit)

阶段 5 的修复涉及 3 个文件,每个都是独立的"一刀",分开提交。`demo_orphan_inode.py` 是这次 bug 的复现脚本,跟修复属于同一主题,一并入库:

```bash
# Commit 1: paths.py 引入 META_DIR
git add apps/platform/src/od_platform/common/paths.py
git commit -m "feat(common): introduce META_DIR for tool self-metadata

Adds a separate namespace for meta-tool artifacts (audit logs of
reset/doctor/upgrade etc.) at .odp-meta/, distinct from business
logs at LOGGING_DIR. The two have different lifecycles:
  - LOGGING_DIR: business runtime logs, cleaned by reset
  - META_LOGGING_DIR: meta-tool audit trail, never auto-cleaned
This mirrors industry practice (Ansible, Terraform, apt) where
cleanup tools never delete their own logs."

# Commit 2: .gitignore
git add .gitignore
git commit -m "chore: gitignore .odp-meta/ (local meta-tool history)"

# Commit 3: reset v4 + 审计 + Linux 复现脚本
git add apps/platform/src/od_platform/cli/reset_project.py scripts/demo_orphan_inode.py
git commit -m "fix(cli): write reset_project logs to META_LOGGING_DIR + add audit context

Fixes a self-reference bug: reset_project wrote its log into
LOGGING_DIR, one of the dirs it deletes. Result:
  - Windows: WinError 32 (rmtree hits the still-open log file)
  - Linux:   silent log loss (orphan inode after unlink)
Writing to META_LOGGING_DIR (outside reset's scope) fixes the bug
and gains proper audit-trail semantics. _audit_context() records
user / git_rev / argv / cwd / pid so the trail is forensically
useful for 'who deleted my data' incidents."

git push origin main
```

---

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 阶段 6: 工程化——注册 odp-reset 命令

`reset_project` 现在已经稳了,但每次都敲 `python scripts/reset_project.py` 太长。D1 给 init 做过 entry-point,这里如法炮制。

## ▶ Step 6.1: pyproject.toml 加一行

打开 `apps/platform/pyproject.toml`,`[project.scripts]` 段加 `odp-reset`:

```toml
[project.scripts]
odp-init = "od_platform.cli.init_project:initialize_project"
odp-reset = "od_platform.cli.reset_project:main"
```

注意 `odp-reset` 指向的是 `main`(它负责解析 argparse),不是 `reset_project`。

## ▶ Step 6.2: 重新装包并验证

```bash
pip install -e ./apps/platform --force-reinstall --no-deps
```

(entry-point 改了,加 `--force-reinstall --no-deps` 让 pip 重新生成命令脚本。)

⚡ **真撞**:

```bash
# 〔造数据〕
odp-reset                       # dry-run(默认),跟 python scripts/reset_project.py 输出一致,只是命令更短
odp-reset --yes                 # 带交互
odp-reset --yes --force         # 无交互
```

🎉 **`odp-reset` 可用了**——和 `odp-init` 凑成一对对称的命令。

## ▶ Step 6.3: 提交工程化收尾

```bash
git add apps/platform/pyproject.toml
git commit -m "feat(platform): register odp-reset console script"
git push origin main
```

> 🧹 **课后的清理(可选)**: `scripts/make_disaster_data.{sh,ps1}` 和 `scripts/demo_orphan_inode.py` 是为这门课造的"教学道具",课程结束后用不上了,可以 `git rm` 删掉再提交一次,保持主分支干净。临时的东西也该走正式的入库/出库,而不是一直漂在那里被遗忘。(如果后面做课后练习还要用,就先留着。)

---

# 总结:你刚才做了什么?

## 推导链复盘

```
出发点: init 的反向操作——但简单反向会出大事
   ↓ 撞墙①: 真让 docs/ scripts/ 被删,git status 真的一片红
解: paths 加 get_dirs_to_reset(显式白名单)
   ↓ 撞墙②: 真把 ROOT_DIR 加进列表,真把项目根连 .git 删了一遍
解: 加 PROTECTED_DIRS + is_protected(黑名单兜底)
   ↓ 撞墙③+④: 真造 2GB+5000 文件,真看屏幕静止 18 秒;用户跑之前不知道会删什么
解: dry-run 默认 + 交互式 RESET 确认 + 进度反馈   →  v3:第一个安全可提交的版本
   ↓ 撞墙⑤: Windows 真撞 WinError 32,Linux 用 lsof 真看 (deleted)
解: 引入 META_LOGGING_DIR(命名空间分离)+ 审计上下文   →  v4
   ↓ 工程化: pyproject 注册 odp-reset
最终: 完整对称的 init / reset 命令对
```

## commit 节奏感:什么时候才该 commit?

| 阶段 | 版本 | 提交? | 判断标准 |
|---|---|---|---|
| 阶段 1-2 | v0 / v1 / v2 | ❌ 不提交 | 不安全——checkout 出来跑一下就误删 |
| 阶段 4 末 | v3 | ✅ 2 个 commit | 默认 dry-run,首次"checkout 出来不会让人出事" |
| 阶段 5 末 | v4 | ✅ 3 个 commit | 修自指 bug,3 个文件各一刀 |
| 阶段 6 末 | — | ✅ 1 个 commit | 工程化收尾 |

> 💡 **金句**: **"什么时候该 commit?——当'checkout 这个 commit 出来不会让任何人出事'的那一刻。"** 普通业务代码"做一段就提交"是合理的;但带 delete/remove/clean/reset 字样的工具,**commit 的时机本身就是安全设计的一部分**。

## 5 次撞墙 = 5 个工业级设计原则

这 5 面墙不是 ODPlatform 独有的——它们是**任何危险操作**共通的设计原则:

| 撞墙 | 设计原则 | 在哪些工具里能看到 |
|---|---|---|
| ① init/reset 不对称 | **白名单优于黑名单** | AWS IAM、防火墙规则 |
| ② 白名单可能错 | **多层防御(defense in depth)** | OAuth scope + RBAC |
| ③ 用户不知道会发生什么 | **dry-run + 显式确认** | `git clean -n`、`terraform plan`、`kubectl --dry-run` |
| ④ 大操作没反馈 | **进度反馈 + 可中断** | apt 安装进度、git clone 进度 |
| ⑤ 工具污染自己 | **审计追踪 + 命名空间分离** | Ansible、Terraform、Alembic、apt |

> 💡 **金句**: **"危险操作的设计,本质是在和【未来犯错的自己】+【粗心的同事】+【惊慌的运维】打交道。多写一行确认,可能避免一次灾难。"**

## init / reset 命令对的对称美

```
            init_project                 reset_project
            ────────────                 ─────────────
目的         创建运行时目录               删除运行时目录
数据源       get_dirs_to_initialize       get_dirs_to_reset
默认行为     直接执行(创建是无害的)      dry-run(删除是危险的)
保护机制     mkdir(exist_ok=True)         is_protected + 二次确认
日志位置     LOGGING_DIR/init_project/    META_LOGGING_DIR/reset_project/
                                          ↑ 注意!不在 LOGGING_DIR——这是审计追踪原则
CLI 命令     odp-init                     odp-reset
```

**看"默认行为"那一行**——这是设计的精髓:创建是幂等且无害的 → 默认就执行;删除是不可逆且危险的 → 默认 dry-run。"**操作的危险程度决定默认行为**",工程界叫 **safe by default**。

**看"日志位置"那一行**——init 的日志写在业务目录,会被 reset 清理(它是业务运行历史);reset 的日志写在元数据目录,**永远不会被 reset 清理**(它是 reset 操作本身的审计追踪)。这种"业务数据"和"工具元数据"严格分离,是企业级工具的标志——它对应的是合规性、可审计性、可追溯性这些"出事时才显现重要性"的能力。

---

# 课后练习

## 练习 1: 撞墙⑥——Windows 只读文件(进阶,真撞)

v3/v4 的 `_on_rm_error` 已经处理了 Windows 只读文件,但你**没真撞过这次墙**:

1. Linux/macOS 上 `./scripts/make_disaster_data.sh` 后 `chmod -w runs/exp_2026_05_10/best.pt`
2. 暂时把 `shutil.rmtree(path, onerror=_on_rm_error)` 改回 `shutil.rmtree(path)`
3. 跑 `odp-reset --yes --force`,看两个平台的行为差异(Windows 上会 `PermissionError`)
4. 把 `_on_rm_error` 加回来,确认 Windows 上能正确恢复

写一份 1 页报告记录两平台差异——这就是撞墙⑥。

## 练习 2: 安全机制压力测试(必做)

故意尝试绕过保护,记录预期 vs 实际:

1. 往 `get_dirs_to_reset()` 加 `ROOT_DIR` / `DOCS_DIR` / `META_LOGGING_DIR`,看 `is_protected` 能不能逐一拦住
2. 把 `.gitignore` 删了再跑 reset(应该没影响——reset 不依赖 .gitignore)
3. 交互确认那里分别输入 `reset`(小写)、带空行、空字符串、Ctrl+C——确认都会取消

## 练习 3: 用审计日志破"事故案"(必做)

1. 先跑几次 `odp-reset --yes --force`,让 `.odp-meta/logs/reset_project/` 里积累几条记录
2. **模拟事故**:故意 `rm -rf data/raw/`(直接 rm,**不通过 reset**)
3. 假装你是值班工程师收到 issue:"我的 raw 数据没了!是不是 reset 干的?"
4. **从审计日志证明**这次不是 reset 干的——找出最后一次 reset 的时间 / argv / git commit,对照 raw 数据消失时间和文件系统证据(`stat data/`)
5. 写一份 1 页"事故根因报告"(参考真实 RCA 文档格式)

> 💡 这个练习让你从"工具的设计者"切换到"事故的调查者"视角。设计者看"我加了什么防御",调查者看"出事时我手上有什么证据"。两边都做过的工程师,才能设计出真正抗事故的工具。

## 练习 4: 设计深度思考(选做)

1. 为什么 `is_protected()` 要检查"路径是不是受保护目录的祖先"?举一个具体攻击场景。
2. 为什么确认词是 `RESET` 而不是 `y`?(提示:`y` 多容易误按,`RESET` 全大写的"打字摩擦"价值)
3. 如果未来加了 `web-backend` 端,它有自己的 `apps/web-backend/logging/`——现在的 reset 能不能正确处理?不能的话怎么改?
4. 假设 6 次 commit 你**全憋到最后一口气提交**,会有什么坏处?(提示:`git bisect` 找回归 bug、code review 难度、出事时 revert 的颗粒度)

## 练习 5: 元工具家族(选做)

未来 ODPlatform 还会有 `odp-doctor`(检查环境健康)、`odp-upgrade`(升级项目结构)、`odp-export`(导出训练结果)。**它们的日志该写在哪里?** 如果你的答案是 `META_LOGGING_DIR/<tool_name>/`——恭喜,你已经把撞墙⑤的教训内化成思维模式了。

## 练习 6: 跟着流程自己做一遍(必做)

不要复制粘贴。按"撞墙→修复"的叙事,把 reset_project 自己实现一遍。**关键:每一次撞墙都真撞,不要跳过灾难脚本**。尤其阶段 5 的双平台撞墙——Windows 看 `WinError 32`、Linux 看 `(deleted)`——是这一节最有价值的部分,强烈建议两个平台都完整经历一次,再做修复。

---

# 给你的最后一段话

reset_project.py 是个**只有 250 行代码的小工具**。但里面藏着 5 个工业级设计模式——这些模式你以后写**任何"危险操作"**(数据库迁移、用户删除接口、文件清理脚本、备份恢复……)都用得上。

**安全的工具,不是【看起来不会出错】的工具,而是【出错时影响最小、出错后能查清】的工具。**

reset 教给你的不只是"怎么删目录",而是:

> 1. **白名单永远比黑名单安全**——明确说"能做什么",而不是猜"不能做什么"
> 2. **多层防御**——单点保护总会失效,要叠加
> 3. **safe by default**——危险操作默认应该只读 / dry-run,显式才执行
> 4. **透明优于神秘**——告诉用户你要做什么,让他有机会喊停
> 5. **进度反馈是用户体验的一部分**——没反馈的工具,再正确也让人焦虑
> 6. **危险操作的 UI 不该好看,该突兀**——视觉打断让用户从"扫读"切到"决策"
> 7. **工具的元数据要和业务数据分开**——审计追踪不能被审计对象自己销毁
> 8. **看清 bug 真正在哪一刀**——有时不是代码写错,而是架构里少了一层
> 9. **commit 时机本身就是安全设计**——不安全的中间版本不该污染 git 历史

下次你要写**任何**带 `delete` / `remove` / `clean` / `reset` 名字的功能,把这个清单拿出来对一遍——你会感谢今天的自己。

**5 次撞墙,5 个层次**:数据正确性(白名单)→ 防御深度(黑名单)→ 用户透明(dry-run + 确认)→ 操作体验(进度反馈)→ **工具自身的位置感(命名空间分离 / 审计追踪)**。前 4 个是"工具对世界的姿态",第 5 个是"工具对自己的认知"。能想到第 5 个的工程师,就能开始设计平台级工具了。

---

*项目: ODPlatform | 对应文档: D2 reset_project 反向工程练习*
*配套上一节: 《D1 项目初始化:common/ 层的诞生》*
*配套下一节: 《D3 数据流水线:data_pipeline 子系统》*
*配套源码: paths.py / reset_project.py / scripts/make_disaster_data.{sh,ps1} / scripts/demo_orphan_inode.py*
